/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['static.website-files.org', 'drz0f01yeq1cx.cloudfront.net', 'ik.imagekit.io']
    }
};

export default nextConfig;
